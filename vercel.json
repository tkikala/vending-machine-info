{
  "buildCommand": "cd api && npm install && npx prisma generate --schema=./schema.prisma && cd ../frontend && npm install && npx vite build",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/**/*.ts": {
      "runtime": "@vercel/node@5.3.7",
      "maxDuration": 60
    }
  }
} 